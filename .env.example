NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Clé SECRÈTE (Supabase → Settings → API Keys → Secret keys, sb_secret_…).
# Serveur uniquement : sert à créer les comptes facilitateurs et à
# réinitialiser les mots de passe. JAMAIS de préfixe NEXT_PUBLIC_, jamais
# commitée — le dépôt est public.
SUPABASE_SECRET_KEY=
